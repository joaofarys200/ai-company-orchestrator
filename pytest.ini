[pytest]
testpaths = tests
markers =
    unit: deterministic unit tests without external services
    integration: isolated integration tests without production side effects
    live_model: tests that require a locally available model runtime
    fault_injection: deterministic failure and recovery tests

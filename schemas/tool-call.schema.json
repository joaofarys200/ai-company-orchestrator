{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "jarvis/tool-call/v1",
  "title": "ToolCall",
  "description": "Contract for agent tool invocations executed in the JARVIS OS tool registry.",
  "type": "object",
  "required": ["tool_name", "parameters"],
  "properties": {
    "tool_name": {
      "type": "string",
      "minLength": 1,
      "description": "Registered name of the tool (e.g. read_file, save_project_file, search_arxiv)."
    },
    "parameters": {
      "type": "object",
      "description": "Keyword arguments passed to the tool function."
    },
    "call_id": {
      "type": "string",
      "description": "Optional unique call identifier for correlation."
    },
    "agent_name": {
      "type": "string",
      "enum": ["Clara", "Alex", "Devon", "Quinn", "User", "System"],
      "description": "Agent initiating the tool call."
    }
  },
  "additionalProperties": false
}
